import React from 'react'

const PrintAble = (props) => {
  return (
    <div id={props.printableId} className={props.printableId}>
        <h3>Ticket Number</h3>
        <h2>123</h2>
    </div>
  )
}

export default PrintAble
